import { User } from './user.entity';
import { EmailMetadata } from './email-metadata.entity';
import { EmailContent } from './email-content.entity';

export const entities = [User, EmailMetadata, EmailContent];
